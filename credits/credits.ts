// 积分系统占位 - 暂不启用

export async function addCredits(params: any): Promise<void> {
  // 暂不实现
  console.log('[Credits] addCredits called but not enabled');
}

export async function addSubscriptionCredits(
  userId?: string,
  priceId?: string
): Promise<void> {
  // 暂不实现
  console.log('[Credits] addSubscriptionCredits called but not enabled');
}

export async function addLifetimeMonthlyCredits(
  userId?: string,
  priceId?: string
): Promise<void> {
  // 暂不实现
  console.log('[Credits] addLifetimeMonthlyCredits called but not enabled');
}
